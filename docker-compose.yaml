services:
  postgres:
    image: postgres:16-alpine
    container_name: dbp-project-backend
    ports:
      - "${DB_PORT:-5432}:5432"
    environment:
        POSTGRES_DB: ${DB_NAME}
        POSTGRES_USER: ${DB_USER}
        POSTGRES_PASSWORD: ${DB_PASSWORD}
